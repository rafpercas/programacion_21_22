package U7.T1.a3;

import U7.T1.a3.Numero;

import java.util.ArrayList;

//Actividad 3: Crear una colección de 20 números enteros aleatorios menores
// que 10, guardarlos por orden decreciente a medida que se vayan generando y mostrar la colección por pantalla.
public class Main {
  public static void main(String[] args) {
    ArrayList<Numero> lista = new ArrayList<>();
    lista.add(new Numero());
    for (int i = 0;i < 20; i++) {
      Numero numeroAIntroducir = (new Numero());
     // if(numeroAIntroducir>=lista){

      }
    }

  }

